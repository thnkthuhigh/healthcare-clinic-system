{
  "name": "@clinic/backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "node scripts/dev-with-env.mjs",
    "build": "mvn -DskipTests package",
    "test": "mvn test"
  }
}
